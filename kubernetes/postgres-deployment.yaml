apiVersion: apps/v1
kind: Deployment
metadata:
  name: repeatwise-postgres
  namespace: repeatwise
  labels:
    app: repeatwise-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: repeatwise-postgres
  template:
    metadata:
      labels:
        app: repeatwise-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "repeatwise_db"
        - name: POSTGRES_USER
          value: "giapnt"
        - name: POSTGRES_PASSWORD
          value: "abcd1234"
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        - name: postgres-init
          mountPath: /docker-entrypoint-initdb.d
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - giapnt
            - -d
            - repeatwise_db
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - giapnt
            - -d
            - repeatwise_db
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-pvc
      - name: postgres-init
        configMap:
          name: postgres-init-config
---
apiVersion: v1
kind: Service
metadata:
  name: repeatwise-postgres
  namespace: repeatwise
spec:
  selector:
    app: repeatwise-postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: repeatwise
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
