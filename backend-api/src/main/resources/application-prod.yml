spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.repeatwise: INFO
    org.springframework.web: INFO

springdoc:
  swagger-ui:
    enabled: false
